export type Color = {
    label: string;
    shades: Array<string>;
};

export type RGB = {
    r: number;
    g: number;
    b: number;
};

export type Settings = {
    offWhite: string;
    offBlack: string;
};
